# Copy to .env (never commit .env). Get a token at https://account.mapbox.com/
PUBLIC_MAPBOX_ACCESS_TOKEN=

# For GitHub Pages: add the same value as a repo secret named PUBLIC_MAPBOX_ACCESS_TOKEN
# (Settings → Secrets and variables → Actions → New repository secret).
